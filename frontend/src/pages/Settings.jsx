/**
 * Settings Page
 *
 * Walking Skeleton stub — system configuration and user preferences.
 * Will display: irrigation thresholds, schedule management, user profile, MQTT broker status.
 */
function Settings() {
  return (
    <div className="page page--settings">
      <h2 className="page__title">Settings</h2>
      <p className="page__placeholder">
        System configuration, schedules, and user preferences will appear here.
      </p>
    </div>
  );
}

export default Settings;
